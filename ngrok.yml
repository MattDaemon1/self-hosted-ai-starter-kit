version: "2"
authtoken: ${NGROK_AUTHTOKEN}

web_addr: 0.0.0.0:4040

tunnels:
  n8n:
    addr: caddy:80
    proto: http
    inspect: true

  n8n-https:
    addr: caddy:443
    proto: http
    inspect: true

  ollama:
    addr: caddy:8080
    proto: http
    inspect: true

  qdrant:
    addr: caddy:8081
    proto: http
    inspect: true

log_level: info
region: eu
